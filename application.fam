App(
    appid="actionstest",  # Must be unique
    name="[ASDF] AppName",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="actionstest_app",
    stack_size=2 * 1024,
    fap_category="GPIO",
    fap_version=(0, 1),
    fap_icon="actionstest.png",
    fap_icon_assets="images",  # Image assets to compile for this application
)
